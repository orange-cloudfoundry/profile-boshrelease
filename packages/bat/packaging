set -ex

tar xf bat.tar.gz
version=$(find . -mindepth 1 - maxdepth 1 -type d -name "bat-*" | grep -Po "v[\d]+(\.[\d]+){2}")
pushd bat-"$version"-x86_64-unknown-linux-gnu
  install -D ./bat "$BOSH_INSTALL_TARGET"/bin/bat
popd
